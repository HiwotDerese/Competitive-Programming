class StaticArrays:

    def __init__(self, arr = [0, 0, 0, 0], capacity = 4, length = 0):
        self.index = 
        self.value = arr[index]
        self.length = len(arr)
    
    def insertEnd(self, value):
        self.arr[self.length - 1] = value
        length += 1

    def removeEnd(self):
        self.arr[self.length-1] = 0
        self.length -= 1
    
    def insertMiddle(self, index, value):


    def removeMiddle(self, index):

    
    def printArr(self):


# insertEnd(1),insertEnd(3), insertEnd(15) = 1,3,15


